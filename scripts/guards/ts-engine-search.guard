# GUARD ts-engine-search
#
# cmd+F must open the scrollback search so typed text feeds the needle
# instead of the shell. The break drops the open, so "bet" goes to the ring.
test: cmd+F opens the scrollback search, typing feeds the needle, Escape closes it
build: test -Dtypescript-spike=true -Dplatform=null
diff --git a/src/cockpit/native/ts_engine.zig b/src/cockpit/native/ts_engine.zig
index c5489f5..b71299d 100644
--- a/src/cockpit/native/ts_engine.zig
+++ b/src/cockpit/native/ts_engine.zig
@@ -298,7 +298,7 @@ pub const Engine = struct {
                 pane.selecting = false;
                 pane.session.clearSelection();
             }
-            pane.session.searchOpen();
+            if (false) pane.session.searchOpen();
             return;
         }
         if (primary and !mods.alt and !mods.control and keyIs(event.key, "g")) {
