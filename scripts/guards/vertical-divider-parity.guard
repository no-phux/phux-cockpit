# GUARD vertical-divider-parity
#
# Describe the defect this test exists to catch, and what removing the
# fix below puts back. Prose here is preserved across re-runs.
test: vertical divider drag keyboard accessibility and child rectangles share resolved geometry
red: 2026-09-03 at 81b2161
diff --git a/src/cockpit/native/view.zig b/src/cockpit/native/view.zig
index 28e7022..87f802b 100644
--- a/src/cockpit/native/view.zig
+++ b/src/cockpit/native/view.zig
@@ -1195,7 +1195,7 @@ fn paneSubtree(
             return ui.split(.{
                 .split_axis = switch (entry.orientation) {
                     .horizontal => .horizontal,
-                    .vertical => .vertical,
+                    .vertical => .horizontal,
                 },
                 .grow = 1,
                 .min_width = split_pane_min_width,
